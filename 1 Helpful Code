

## GCF:
def reduce_fraction(fraction):
    # your code here
    a = fraction[0] # first number
    b = fraction[1] # second number

    while b != 0:
        a, b = b, a%b
        
    return (int(fraction[0]/a), int(fraction[1]/a))
